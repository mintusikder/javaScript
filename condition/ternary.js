/**
 *
 * Ternary ----> three part
 *
 * condition  ? do something when true   :  do something when false
 *
 */
const age = 12;
age >= 18 ? console.log("vote dio") : console.log("Gumay thako");


let price = 100 ;
const isLeader = false;

price = isLeader === true ? 0 : price + 100;